## [08:05] Find Hitler Phase 4 — Node Graph + Leaderboard

- Installed D3 v7; added `src/lib/resultColors.js` (finish-order → hex color)
- Added `src/lib/pathGraph.js` — `buildGraph(finishers)` merges shared nodes into a D3-ready graph (Map keyed on normalized title, isStart/isTarget flags, 20-char label truncation); 6 unit tests
- Added `src/lib/leaderboard.js` — `addEntry`/`getEntries`, localStorage, 50-entry LRU cap (drops lowest-score among existing, never the just-added entry), SSR guard via `typeof localStorage`; 6 unit tests
- Added `src/components/NodeGraph.jsx` — D3 force simulation (link/charge/center/collision), D3 computes positions, React renders SVG; useMemo in ResultsScreen prevents sim thrashing on state changes
- Added `src/components/ResultsScreen.jsx` — header, D3 node graph, finisher rows with medals+colors, Copy Path + Share + Play Again buttons with clipboard try/catch
- Added `src/app/results/page.jsx` — reads/clears `sessionStorage['gameResults']`, redirects if absent
- Added `src/components/Leaderboard.jsx` — table (8 columns) + empty state
- Added `src/app/leaderboard/page.jsx` — reads localStorage entries via `getEntries()`
- Modified `src/app/play/page.jsx` — captures `data.path` on WIN via `playerNameRef`, writes sessionStorage + leaderboard, passes `onViewResults` to WinScreen
- Modified `src/components/WinScreen.jsx` — "View Node Graph →" button (hidden on timeUp)
- Modified `src/app/play/multi/page.jsx` — `handleViewResults` maps finishers state, fallback from `myFinish` if no isMe entry, writes sessionStorage + leaderboard
- Modified `src/components/MultiWinScreen.jsx` — "View Results →" button
- Modified `src/app/page.jsx` — "Leaderboard →" nav link top-right
- Tests: 73/73 passing (11 test files); 11 commits on master
- Smoke test: requires manual run (`npm run dev`, port 3003) — server can't start in CI bash on Windows

## [06:10] Task 4: NodeGraph D3 force simulation rendered as React SVG

- Created `src/components/NodeGraph.jsx` — client component running a D3 v7 force simulation (link, charge, center, collision forces), rendering settled positions as React SVG
- Nodes colour-coded: start = gold stroke (#f1c40f), target = red fill (#c0392b), intermediates = dark fill (#1a1a2e)
- Links render thicker / more opaque when `finishIndex === 0` (winner path highlight)
- Deep-clones props before handing to D3 so React state objects are never mutated
- Simulation stopped on unmount via useEffect cleanup
- Files changed: `src/components/NodeGraph.jsx` (new), `logs/2026-06-01.md`

## [06:03] Find Hitler Phase 3 — All Game Modes
- Added Golf Mode (5-min cap, click scoring, countdown HUD)
- Added 5 Clicks to Jesus (par scoring, target locked, round indicator, par-grade WinScreen)
- Added Hardcore Modifier (0 undos, halved time caps)
- Added Daily Challenge (seeded RNG, 31 pre-curated pairs)
- Added No-Hub Challenge (174 hub blocklist, O(1) lookup, bounce mechanic + banner)
- Added curated Speedrun hubs per target (5 start pages × 8 targets)
- Files: src/lib/hubBlocklist.js, speedrunHubs.js, dailyChallenge.js; scoring.js; gameState.js; start/route.js; move/route.js; page.jsx; GameHUD.jsx; WinScreen.jsx; play/page.jsx; tests/hubBlocklist.test.js, dailyChallenge.test.js, phase3.test.js, scoring.test.js, gameState.test.js
- Tests: 61/61 passing (9 test files); merged feat/phase-3-modes → master

## [04:42] Task 4: Add Golf and Jesus Par scoring functions

- Added `calculateGolfScore` function to src/lib/scoring.js
- Added `calculateParGrade` function with golf scoring grades (Hole-in-One, Albatross, Eagle, Birdie, Par, Bogey, Double Bogey)
- Appended 8 new test cases to tests/scoring.test.js
- All 12 tests pass (4 existing + 8 new)
- Files changed: src/lib/scoring.js, tests/scoring.test.js
- Committed with: feat: add golf and jesus par scoring to scoring.js (c49d508)
