# Find Hitler / Six Clicks — 2026-06-03

## Fact Checker mode — real Wikipedia pipeline rebuild (subagent-driven execution)

Executed the approved plan `docs/superpowers/plans/2026-06-02-fact-checker-real-wikipedia-pipeline.md` in an isolated worktree (`feat+fact-checker-wikipedia`, branched off clean `origin/master`) because a concurrent six-degrees session was live in the main checkout. 13 tasks, dispatched one implementer subagent per task with verification between.

### What was built
- **Bug fix (the headline):** scoring no longer compares strings. `scoreAccusation({fcId|selection}, mistakes, difficulty)` matches by `data-fc-id` (clicks) or guarded text-overlap (free-select). The original unwinnable "20 April 1879" vs mistake-key "1879" desync is now structurally impossible — regression test added.
- **Schema:** `FactCheckArticle` gains `difficulty`, `decoys`, `sourceTitle`, `sourceUrl`; legacy `spans` column dropped (anti-pattern). `prisma db push` applied to live Supabase (required `--accept-data-loss` for the 1 legacy row's spans — that row is being retired anyway).
- **Pipeline (`src/lib/factCheckerGen.js`):** `trimByDifficulty` (strips infobox, lead-only→full by difficulty), `htmlToPlainText`, `wrapAndValidate` (deterministically wraps located phrases in `<span data-fc-id data-fc-mistake>`, DISCARDS any phrase it can't locate → reachability guarantee), `stripTruthForClient` (anti-cheat: easy/medium drop `data-fc-mistake`, hard/hardcore unwrap entirely), `generateTamperedArticle` (fetch→trim→LLM→wrap, best-of-3-attempts).
- **LLM (`src/lib/llm.js`):** `buildTamperPrompt` + `parseTamperJson` (pure, tested) + `callTamperLLM` (Claude `claude-sonnet-4-6`, thin wrapper). LLM only returns plain-text find/replacement phrases; pipeline does all HTML mutation.
- **Routes:** article GET filters by `difficulty` and strips truth per difficulty (never ships `mistakes`/`decoys` or the truth flag); accuse POST takes `fcId|selection`, real difficulty, identity-Set `allFound`.
- **UI:** `FactCheckerArticle` renders pre-stripped HTML — clickable chips (easy/medium) with real affordance, free drag-select (hard/hardcore) with coarse client guard; play page rewired to `{fcId|selection}` payloads + `foundId` tracking + Wikipedia CC BY-SA source footer; HUD reads `.label`.
- **Generation tooling:** admin `/admin/fact-checker` Generate button (subject/random + difficulty + category → pending) with decoy preview; `scripts/generate-fact-checker.mjs` batch generator (replaces deleted `seed-fact-checker.mjs`); `ANTHROPIC_API_KEY` documented in DEPLOY.md.

### Tests
- New/rewritten: `tests/factChecker.test.js` (fcId + overlap + regression), `tests/factCheckerGen.test.js` (trim, wrap/validate/discard, strip-truth, prompt/parse, orchestration w/ mocked deps).
- Full suite: **152 passed, 1 failed** — the 1 failure is the pre-existing, out-of-scope `wikipedia.test.js > "strips navbox, infobox, and reflist"` red (wikipedia.js intentionally keeps infobox for the racing game; untouched here).
- Lint: my code clean; 2 pre-existing errors in admin page (lines 10/25, not my additions) left as-is.

### Plan correction caught during execution
The plan's over-select guard test miscounted "20 april 1879" as 12 chars (it's 13), making the ×3 guard reject a legitimate selection. Widened guard to ×4 in code; reconciled plan + spec docs.

### NOT YET DONE — human checkpoint required
- `.env.local` has **no `ANTHROPIC_API_KEY`** → live generation can't run. User must add it.
- Then: run the batch generator (or admin button) to create a library, approve in `/admin/fact-checker`, and play-verify each difficulty in the browser (clickable affordance, found-count reaches N/N, hard-mode free-select + over-select guard, no `data-fc-mistake` in client DOM).
- Retire the legacy hand-seeded "Adolf Hitler" row (reject/delete) so it never serves.

### Files changed
prisma/schema.prisma; src/lib/factChecker.js; src/lib/factCheckerGen.js (new); src/lib/llm.js (new); src/app/api/fact-checker/{article,accuse}/route.js; src/app/api/admin/fact-checker/route.js; src/app/admin/fact-checker/page.jsx; src/components/FactCheckerArticle.jsx; src/components/FactCheckerHUD.jsx; src/app/play/fact-checker/page.jsx; scripts/generate-fact-checker.mjs (new); scripts/seed-fact-checker.mjs (deleted); DEPLOY.md; tests/factChecker.test.js; tests/factCheckerGen.test.js (new).

## Fact Checker — $0 LLM swap (cost-cut) + provider findings

Reworked tampering to cost $0: `src/lib/llm.js` now calls any OpenAI-compatible `/chat/completions`
endpoint via `fetch` (config: `FC_LLM_BASE_URL` / `FC_LLM_MODEL` / `FC_LLM_API_KEY` / `FC_LLM_TIMEOUT_MS`
/ `FC_LLM_MAX_TOKENS`). Default = free local Ollama. Dropped `@anthropic-ai/sdk`. Committed d751977.
Hardening: undici long-timeout dispatcher + AbortSignal (Ollama buffers full completion → tripped the
default 300s headersTimeout); configurable max_tokens (thinking models eat budget); `<think>` stripping;
retry on transient parse failures; exponential backoff on 429/503. 41 unit tests green.

Provider reality (all verified live this session):
- **Local Ollama**: works, $0, unlimited — but ~29 min/article (machine is 100% CPU, no GPU) and weak
  quality (whole-sentence spans, hallucinated decoys that get discarded). Last resort only.
- **Gemini 2.5 Flash** (key in api keys/gemini apik.txt, valid): EXCELLENT quality + fast — verified one
  full end-to-end generation (real Wikipedia → clever traps like "4 July 1934"→Pierre's death date + real
  decoys). BUT free tier = `GenerateRequestsPerDayPerProjectPerModel-FreeTier` limit **20/day**, which my
  diagnostics exhausted today. Resets next day. `gemini-2.0-flash` 429s separately; use **gemini-2.5-flash**.
- **OpenRouter**: `api keys/openrouter apik.txt` is EMPTY → 401. Unusable until a key is added.
- **DeepSeek**: key present but paid (not $0).

Decision: **defer generation to tomorrow via Gemini** (quota reset). `.env.local` already has the Gemini
FC_LLM_* config (gitignored). 

### RESUME TOMORROW (Gemini quota reset)
From the worktree `C:\Users\lasse\Desktop\find-hitler\.claude\worktrees\feat+fact-checker-wikipedia`:
1. `node --env-file=.env.local scripts/generate-fact-checker.mjs --count 5 --difficulty easy` (repeat for
   medium/hard/hardcore). NOTE Gemini free = 20 req/day — keep total ≤ ~15 articles/day, or switch
   FC_LLM_MODEL/provider. Each generate = 1 req (+retries only on failure).
2. Review/approve in `/admin/fact-checker` (PORT=3014 npm run dev; 3004 is the concurrent six-degrees session).
3. Play-verify each difficulty at `/play/fact-checker?difficulty=...`: chips clickable; found reaches N/N
   (the original bug); hard mode free-select + over-select guard; no data-fc-mistake (medium)/data-fc-id (hard)
   in client DOM.
4. Then merge the branch (finishing-a-development-branch). Legacy "Adolf Hitler" row already retired (rejected).
